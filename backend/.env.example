# Copy to backend/.env and adjust. Never commit .env — it is gitignored.
APP_NAME=AeroPulse
DEBUG=true

# SQLite for local development; *.db is gitignored.
DATABASE_URL=sqlite:///./aeropulse.db

# Frontend origin allowed to call this API.
CORS_ORIGINS=http://localhost:3000
